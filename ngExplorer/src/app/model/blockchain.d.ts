import { Block } from "./block";

export class Blockchain {
    constructor();
/*    addBlock({data, address: string, balance: number}): void;
    replaceChain(chain: Blockchain, validTransactions: Transaction, onSuccess: any): void;
    validTransactionData(chain: Blockchain): Boolean;
    isValidChain(chain: Blockchain): Boolean;
    getDifficulty (chain: Blockchain): number;
    getAdjustedDifficulty (latestBlock: Block, chain: Blockchain): number;
    getCumulativeDifficulty(chain: Blockchain): number;
*/
}

/*
export interface Transaction {
    id: string;
    outputMap: OutputMap;
    input: Input;
}

export interface OutputMap {
    [id: string]: number
}

export interface Input {
    timestamp: number;
    amount: number;
    address: string;
    signature: Signature;
}

export interface Signature {
    r : string;
    s : string;
    recoveryParam : number
}
*/